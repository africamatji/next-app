import NavStyles from '../styles/Nav.module.css';

const QuizzesList = ({quizzes}) => {
    return (
        <div className={NavStyles.nav}>
            <ul>
                <li>Posts will be display here...</li>
            </ul>
        </div>
    );
};

export default QuizzesList;